changelog:
- type: FIX
  description: add back the virtualservice create consul related flags
  issueLink: https://github.com/solo-io/gloo/issues/1172
- type: FIX
  description: if we run glooctl uninstall -all with a nonexistent namespace, still uninstall everything instead of erroring out.
  issueLink: https://github.com/solo-io/gloo/issues/602
- type: FIX
  description: for both glooctl add and glooctl create, a --dry-run will output a kube-yaml file unless the --output flag overrides it.
  issueLink: https://github.com/solo-io/gloo/issues/907
